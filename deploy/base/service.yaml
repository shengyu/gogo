apiVersion: v1
kind: Service
metadata:
  name: gin-api
  labels:
    app.kubernetes.io/name: gin-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gin-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
